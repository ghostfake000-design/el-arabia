name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # بيسرع البلد المرات الجاية

      - name: Install dependencies
        run: npm ci

      - name: Build (electron-builder)
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }} # مهم جداً يكون هنا كمان
        run: npm run build

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: v1.0.${{ github.run_number }}
          name: "Release v1.0.${{ github.run_number }}"
          body: "Automated build with digital signature"
          draft: false
          prerelease: false
          # السطر اللي جاي ده هو اللي بيرفع الـ EXE للناس:
          artifacts: "dist/*.exe,dist/*.msi" 
          token: ${{ secrets.GH_TOKEN }}

      - name: Upload Artifacts (Internal)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/*.exe