name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        # استخدمنا install بدل ci عشان يتخطى مشاكل الـ lock file اللي حصلت قبل كدة
        run: npm install

      - name: Build (electron-builder)
        env:
          # السيرفر هياخد الشهادة الجديدة من الـ Secrets بتاعتك
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npm run build

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: v1.0.${{ github.run_number }}
          name: "Release v1.0.${{ github.run_number }}"
          body: "نظام العربية للمخازن - نسخة احترافية موثقة برقم إصدار ${{ github.run_number }}"
          draft: false
          prerelease: false
          # الملفات هتطلع في الفولدر ده بناءً على إعدادات package.json
          artifacts: "dist-electron/*.exe" 
          token: ${{ secrets.GH_TOKEN }}